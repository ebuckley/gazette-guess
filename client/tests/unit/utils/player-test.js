import player from 'client/utils/player';
import { module, test } from 'qunit';

module('Unit | Utility | player');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = player();
  assert.ok(result);
});
